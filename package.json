{
  "name": "packperfect",
  "scripts": {
    "build": "cd preview && npm install && npm run build",
    "dev": "node api/dev-server.mjs & cd preview && npm run dev",
    "api": "node api/dev-server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.92.0",
    "openai": "^6.36.0"
  }
}
